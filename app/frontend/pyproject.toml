[project]
name = "quiz-frontend"
version = "0.1.0"
description = "Quiz Flet frontend"
requires-python = ">=3.12"
dependencies = [
    "flet[all]>=0.86.5",
    "flet-audio>=0.86.5",
    "flet-video>=0.86.5",
    "httpx>=0.27.0",
    "quiz-shared",
]

[tool.uv]
package = false

[tool.uv.sources]
quiz-shared = { workspace = true }
