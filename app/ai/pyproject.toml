[project]
name = "quiz-ai"
version = "0.1.0"
description = "Quiz AI"
requires-python = ">=3.12"
dependencies = [
    "instructor>=1.15.4",
    "quiz-shared",
]

[tool.uv]
package = false

[tool.uv.sources]
quiz-shared = { workspace = true }
